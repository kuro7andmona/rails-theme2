<h1>New book</h1>
  <% if @book.errors.any? %>
    <%= @book.errors.count %>2 errors prohibited this book from being saved:
    <ul>
      <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    </ul>
  <% end %>
<% end %>

<%= form_with model: Book.new do |f|%>
  <h4>title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_area :body %>

  <div class ="label"><%= f.submit'Create Book' %></div>
<% end %>